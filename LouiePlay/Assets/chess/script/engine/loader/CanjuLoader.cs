
public class CanjuLoader : BaseLoader {
    public void LoadCanju(out CanjuData canju)
    {
        canju = new CanjuData();
        //没完 残局加载
        while (true)
        canju.CanJu.Add("",new MoveData());
    }
}
